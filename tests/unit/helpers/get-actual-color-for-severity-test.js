import { getActualColorForSeverity } from 'dr-elephant/helpers/get-actual-color-for-severity';
import { module, test } from 'qunit';

module('Unit | Helper | get actual color for severity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getActualColorForSeverity([42]);
  assert.ok(result);
});
