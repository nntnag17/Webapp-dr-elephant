import { getPages } from 'dr-elephant/helpers/get-pages';
import { module, test } from 'qunit';

module('Unit | Helper | get pages');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getPages([42]);
  assert.ok(result);
});
