import { getAcutalColorForSeverity } from 'dr-elephant/helpers/get-acutal-color-for-severity';
import { module, test } from 'qunit';

module('Unit | Helper | get acutal color for severity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getAcutalColorForSeverity([42]);
  assert.ok(result);
});
