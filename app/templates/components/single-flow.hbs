

          <table class="app-summary-table box shadow">
        <tr>

            <td class="severity-side-blob-generic severity-side-blob-{{getColorForSeverity flow.severity}}">
            </td>
            <td>
          {{#link-to "workflow" (query-params workflowid=flow.id) class="list-group-item list-group-item-control"}}


                      <table class="app-header">
                          <tr>
                              <td class="heading">
                                {{flow.id}}
                              </td>
                              <td class="user"><span class="glyphicon glyphicon-user"></span>  {{flow.username}}</td>
                          </tr>
                      </table>

                  <table class="borderless">
                      <tbody>
                      <tr>
                          <td class="date-header">
                            {{getDate flow.finishtime}}
                          </td>
                      </tr>

                      <tr>
                          <td class="job-severity-block">{{jobs-severity flow=flow}}</td>
                      </tr>

                      <tr>
                          <td class="aggregated-metrics-block">{{aggregated-metrics application=flow}}</td>
                      </tr>
                      </tbody>
                  </table>
          {{/link-to}}
            </td>

        </tr>
          </table>


